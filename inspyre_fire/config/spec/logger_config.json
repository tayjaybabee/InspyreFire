{
    "log_level": {
        "type": "string",
        "allowed_values": [
            "DEBUG",
            "INFO",
            "WARNING",
            "ERROR",
            "CRITICAL"
        ],
        "default": "INFO",
        "description": "The log level to use for the logger",
        "required": false
    },
    "log_file": {
        "type": "string",
        "default": null,
        "description": "The file to log to",
        "required": false
    },
    "log_file_level": {
        "type": "string",
        "allowed_values": [
            "DEBUG",
            "INFO",
            "WARNING",
            "ERROR",
            "CRITICAL"
        ],
        "default": "DEBUG",
        "description": "The log level to use for the file logger",
        "required": false
    }
}
